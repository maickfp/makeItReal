// Revisión división
// entradas
var divisor = 9;
var dividendo = 3;

// verificacion del dividor
if(divisor == 0){
    console.log("El divisor debe ser diferente a 0");
// calcular division
}else{
    console.log("El resultado de la división (" + divisor + "/" + dividendo + ") es: " + (divisor/dividendo));
}
